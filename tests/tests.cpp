#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>

TEST_CASE("testing our tests (test (using doctest))") {
    CHECK (true);  // i hope it's true
    CHECK (false);  // preview of testing dania's work
}